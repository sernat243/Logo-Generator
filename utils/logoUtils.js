class Logo {
    constructor(textColor, shape, shapeColor) {
        this.textColor = textColor;
        this.shape = shape;
        this.shapeColor = shapeColor;
    }

    generateSVG() {
        const svgTemplate = `
        `;
        return svgTemplate;
    }
};

module.exports = {
    Logo,
};